{% block content %}
<section>
	<div id="orden-modulo-lista">
		<div class="header-buttons">
			<div class="btn-group main-buttons">
				<a role="button" @click="nueva()" class="btn btn-success">
					<span class=""></span> Realizar orden
				</a>
			</div>
		</div>
		{% include( 'Sale/Partials/list/table.twig' ) %}
        <!-- Modal detail sale -->
        {% include( 'Sale/Partials/list/modalDetails.twig' ) %}
	</div>
</section>
{% endblock %}
{% block loadScripts %}
	<script id="scriptContainer" type="text/javascript">
		uiVars['catalogs'] = {{ catalogs|json_encode|raw }};

		init = [ {
			title: 'Ordenes de venta',
			file: 'Sale/orderList.js',
			functions: [ {
				name: 'iniciarListadoOrdenes',
				args: [ uiVars['catalogs'] ]
			} ]
		} ];
		// initModule( init );
	</script>
{% endblock %}
