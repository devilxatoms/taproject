		<!-- Modal para búsqueda de ventas-->
		<div class="modal-dialog modal-md" role="document">
			<div class="modal-content">
				<div class="modal-header">
					<button type="button" class="close" data-dismiss="modal" aria-label="Close">
						<span aria-hidden="true">&times;</span>
					</button>
					<h4 class="modal-title" id="myModalLabel">
						
					</h4>
				</div>
				<div class="modal-body">
					<div class="col-md-6">
						<div class="form-group">
							<label>Tipo:</label>
							<vue-selectize-js
								v-bind:selectize-settings="selectizeSettings"
								v-bind:selectize-options="selectizeOptions"
								v-model="tipoVenta">
							</vue-selectize-js>
						</div>
					</div>
					<div class="col-md-12">
						<div class="form-group">
							<label>Cliente:</label>
							<input type="text" v-model="nombreCliente" @keyup.enter="buscar()">
						</div>
					</div>
					<div class="col-md-12" style="text-align: right;">
						<a role="button" class="btn btn-success" @click="buscar()">
							<span class="glyphicon glyphicon-search"></span> Buscar
						</a>
					</div>
					<br>
					<div class="col-md-12">
						<vue-dynamic-table class-name="table table-bordered table-hover"
							v-bind:columns="sourceColumns"
							v-bind:data-model="listSales"
							v-bind:filter-enabled="false">
							<template slot="slotRadio" scope="row">
								{#<el-radio
									:label="row.object.id"
									@change="checkTipo(row)"
									v-model="saleId">
								</el-radio>#}
								<input type="radio" 
									:value="row.object.id"
									@change="checkTipo(row)"
									v-model="saleId"
									name="saleId">
							</template>
						</vue-dynamic-table>
					</div>
				</div>
				<div class="modal-footer">
					<button type="button" class="btn btn-default" data-dismiss="modal" @click="aceptar()">Ok</button>
				</div>
			</div>
		</div>
